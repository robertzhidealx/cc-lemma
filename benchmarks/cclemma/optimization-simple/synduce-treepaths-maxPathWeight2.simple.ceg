(
  (data Bool (True False))
  (:: True Bool)
  (:: False Bool)
  (data Unit (Null))
  (:: Null Unit)
  (data Nat (Zero Succ))
  (:: Zero Nat)
  (:: Succ (-> (Nat) Nat))
  (data BTree (Empty Node))
  (:: Empty (-> (Unit) BTree))
  (:: Node (-> (Nat BTree BTree) BTree))
  (data Zipper (Top Left Right))
  (:: Top (-> (Unit) Zipper))
  (:: Left (-> (Nat BTree Zipper) Zipper))
  (:: Right (-> (Nat BTree Zipper) Zipper))
  (:: lq (-> (Nat Nat) Bool))
  (let lq (Zero (Succ ?x)) True)
  (let lq (?x Zero) False)
  (let lq ((Succ ?x) (Succ ?y)) (lq ?x ?y))
  (:: ite2 (-> (Bool Nat Nat) Nat))
  (let ite2 (True ?x ?y) ?x)
  (let ite2 (False ?x ?y) ?y)
  (:: max (-> (Nat Nat) Nat))
  (let max (?tv0 ?tv1) (ite2 (lq ?tv0 ?tv1) ?tv1 ?tv0))
  (:: plus (-> (Nat Nat) Nat))
  (let plus (Zero ?x) ?x)
  (let plus ((Succ ?x) ?y) (Succ (plus ?x ?y)))
  (:: mpath (-> (BTree) Nat))
  (let mpath ((Empty ?tv5)) Zero)
  (let mpath ((Node ?tv6 ?tv7 ?tv8)) (plus ?tv6 (max (mpath ?tv7) (mpath ?tv8))))
  (:: repr (-> (Zipper) BTree))
  (let repr ((Top ?tv12)) (Empty Null))
  (let repr ((Left ?tv13 ?tv14 ?tv15)) (Node ?tv13 ?tv14 (repr ?tv15)))
  (let repr ((Right ?tv16 ?tv17 ?tv18)) (Node ?tv16 (repr ?tv18) ?tv17))
  (:: treerec (-> (BTree) BTree))
  (let treerec ((Empty ?tv22)) (Empty Null))
  (let treerec ((Node ?tv23 ?tv24 ?tv25)) (Node ?tv23 (treerec ?tv24) (treerec ?tv25)))
  (:: ziprec (-> (Zipper) Zipper))
  (let ziprec ((Top ?tv29)) (Top Null))
  (let ziprec ((Left ?tv30 ?tv31 ?tv32)) (Left ?tv30 (treerec ?tv31) (ziprec ?tv32)))
  (let ziprec ((Right ?tv33 ?tv34 ?tv35)) (Right ?tv33 (treerec ?tv34) (ziprec ?tv35)))
  (:: main (-> (Zipper) Nat))
  (let main (?tv36) (mpath (repr (ziprec ?tv36))))
  (:: treerecNew (-> (BTree) Nat))
  (let treerecNew ((Empty ?tv40)) Zero)
  (let treerecNew ((Node ?tv41 ?tv42 ?tv43)) (plus ?tv41 (max (treerecNew ?tv43) (treerecNew ?tv42))))
  (:: mainNew (-> (Zipper) Nat))
  (let mainNew ((Top ?tv47)) Zero)
  (let mainNew ((Left ?tv48 ?tv49 ?tv50)) (plus ?tv48 (max (mainNew ?tv50) (treerecNew ?tv49))))
  (let mainNew ((Right ?tv51 ?tv52 ?tv53)) (plus ?tv51 (max (mainNew ?tv53) (treerecNew ?tv52))))
  (=== synduce_treepaths_maxPathWeight2 (inp0) (Zipper) (main inp0) (mainNew inp0))
)
